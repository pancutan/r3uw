<h1>Rails 3 sobre Windows y Linux Ubuntu</h1>
<h2>Reporte de Errores</h2>
<p></p>
<p>Le agradezco que forme parte del seguimiento de errores y mejora continua de este libro. Toda sugerencia será bienvenida y probablemente corregida :P</p>
<p>Sírvase completar la siguiente forma. No requiere registración.</p>

<%= form_for(@bug) do |f| %>
  <% if @bug.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bug.errors.count, "error") %> prohibited this bug from being saved:</h2>

      <ul>
      <% @bug.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    Nombre / Nombre y Apellido / Nick <br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    Versión del libro
<%= f.select :version_id, @versions.map {|cat| [cat.version, cat.id]} %>

    <%#= f.label :version_id %>
    <%#= f.text_field :version_id %>
  </div>
  <div class="field">
    Página
    <%#= f.label :page %>
    <%= f.text_field :page, :size => 3 %>
  </div>
  <div class="field">
    Tipo de reporte
<%= f.select :type_id, @error_types.map {|cat| [cat.errortype, cat.id]} %>
    <%#= f.label :type_id %><br />
    <%#= f.text_field :type_id %>
  </div>
  <div class="field">
    Descripción
    <%#= f.label :description %><br />
    <%#= f.text_field :description %>

    <%= text_area(:bug, :description, :cols => 40, :rows => 14) %>


  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>


<%= link_to 'Back', bugs_path %>
